import React from "react";
import ReactDOM from "react-dom";

import { App } from "./App";

// /authProvider.getAccountInfo().jwtIdToken
ReactDOM.render(
    <App />,
  document.getElementById("root")
);
